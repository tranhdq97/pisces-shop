# Sao chép thành compose.env (script setup tự làm). Dùng cho biến môi trường container web.
# DATABASE_URL được gán cố định trong docker-compose.yml (host: db).

SECRET_KEY=docker-local-dev-replace-with-a-long-random-string
DEBUG=false
ACCESS_TOKEN_EXPIRE_MINUTES=30
